Lets start the endings:

npm start

Open a browser to http://localhost:3000

it may take a few seconds to spin everything up, 
now you should see a input box, start typing,
if successful, you will see your input box
value be echoed in a h1 tag.

[ctrl]-c will shutdown the beefy web server.

Cow Says: ./n
